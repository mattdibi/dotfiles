/*! @file ${1!substitute(substitute(substitute(expand('%:t'), '\v(<.)(.*)', '\u\1\2', 'g'), '\v\..*', '', 'g'), '\v_(\w)', '\u\1', 'g')}.cpp
 *  @author ${2!git config user.name}
 *  @date   ${3!date +%d/%m/%Y}
 *  @brief $1's class cpp file.
 *
 *  ${4:Detailed description}
 */
